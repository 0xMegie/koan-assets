{
  "name": "@koanprotocol/token-list-api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "check": "tsc --pretty --noEmit",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf .vercel"
  },
  "dependencies": {
    "@vercel/node": "3.0.9"
  },
  "devDependencies": {
    "@tsconfig/esm": "1.0.4",
    "@tsconfig/strictest": "2.0.2",
    "typescript": "5.2.2",
    "vercel": "32.5.3"
  }
}
